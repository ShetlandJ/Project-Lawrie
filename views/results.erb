<a href="/" id="home-btn">⬅ Home</a>
<h2>UK stories:</h2>

<div>
  <% submitted_urls = @scraper_util.return_urls %>
  <% if submitted_urls.length < 1 %>
  <p>You didn't submit any UK news stories!</p>
  <% else %>

  <% submitted_urls.each do |news_item| %>

  <div class="url-item">

    <% if news_item[:news_org].length > 0 %>
    <a href="<%= news_item[:url] %>">
      <p><%= news_item[:content] %> </p>
    </a>
    <p><%= "&nbsp;" + news_item[:news_org] %></p>

    <% else %>
    <p><%= news_item[:content] %> </p></a>
    <p><%= "&nbsp;" + news_item[:news_org] %></p>

    <% end %>

  </div>

  <% end %>
  <% end %>
</div>

<div>
  <% irish_urls = @scraper_util.return_irish_urls %>
  <% if irish_urls.length < 1 %>
  <p>You didn't submit any Irish news stories!</p>
  <% else %>

  <h2>Irish stories:</h2>
  <% irish_urls.each do |news_item| %>

  <div class="url-item">
    <% if news_item[:news_org].length > 0 %>
    <a href="<%= news_item[:url] %>">
      <p><%= news_item[:content] %> </p>
    </a>
    <p><%= "&nbsp;" + news_item[:news_org] %></p>

    <% else %>
    <p><%= news_item[:content] %> </p></a>
    <p><%= "&nbsp;" + news_item[:news_org] %></p>

    <% end %>

  </div>

  <% end %>
  <% end %>
</div>
